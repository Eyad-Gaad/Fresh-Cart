import { ExternalAnchorDirective } from './external-anchor.directive';

describe('ExternalAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new ExternalAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
